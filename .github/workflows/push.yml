name: push

on:
  push:
    branches:
      - master
      - main

jobs:
  # The 22 flake checks, split across runners that go in parallel.
  #
  # This was one job running `nix run path:.#ci`, which is a single derivation
  # depending on every check — so nix built all 22 back to back on one runner and
  # the job took as long as their sum.
  #
  # BUCKETED BY SHARED DEPENDENCY, NOT ONE JOB PER CHECK. A runner starts with an
  # empty /nix/store, so anything a check needs is either fetched from the cache
  # or built again on that runner. Twenty-two jobs would mean twenty-two runners
  # each building the crate artifacts or the NixOS closures their checks sit on,
  # which burns far more total compute for no better wall clock. Grouping checks
  # that share a heavy base means each base is built once per bucket instead:
  #
  #   lint   — no rust, no NixOS eval; these are seconds each and share nothing,
  #            so they cost almost nothing to carry together.
  #   rust   — every crate test and clippy run; crane's cargoArtifacts is shared
  #            between a crate's test and its clippy, so keeping them together is
  #            what makes that sharing pay.
  #   nixos  — the system toplevels, disko scripts, and the unit-ordering checks
  #            that read the evaluated config. All of these force the same NixOS
  #            evaluation and the same closure.
  #
  # `fail-fast: false` because a red bucket should not hide which of the others
  # would also have failed — the point of splitting is to learn more per run.
  checks:
    name: checks (${{ matrix.bucket }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - bucket: lint
            checks: >-
              deadnix hadolint shellcheck formatting chart-checks client-ui
              ci-buckets-cover-every-check
          - bucket: rust
            checks: >-
              local-api-tests installer-tests desktop-client-tests
              wg-register-tests clippy-local-api clippy-installer
              clippy-desktop-client
          - bucket: nixos
            checks: >-
              nixos-create nixos-join nixos-wsl disko-create disko-join
              binary-contract containerd-store-after-order
              self-healing-timers-can-re-arm retry-timers-measure-from-run-end
    steps:
      - uses: actions/checkout@v5

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/boost "$AGENT_TOOLSDIRECTORY" \
            /usr/local/share/powershell /usr/share/swift
          sudo docker image prune --all --force || true
          df -h /

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Build this bucket's checks
        run: |
          set -euo pipefail
          # ONE `nix build` for the whole bucket, never a loop of them: nix
          # schedules the lot together and realises their shared dependencies
          # once, which is the entire reason these are grouped.
          #
          # `path:.#` here MUST match every other nix invocation in this
          # workflow — see the note in the cache step below for what diverging
          # costs.
          targets=()
          for c in ${{ matrix.checks }}; do
            targets+=("path:.#checks.x86_64-linux.$c")
          done
          nix build --no-link --print-build-logs --print-out-paths \
            "${targets[@]}" > /tmp/bucket-out-paths.txt
          cat /tmp/bucket-out-paths.txt

      - name: Push to the Nix cache
        env:
          ATTIC_AUTH_TOKEN: ${{ secrets.ATTIC_AUTH_TOKEN }}
        if: ${{ env.ATTIC_AUTH_TOKEN != '' }}
        run: |
          set -euo pipefail
          nix run nixpkgs#attic-client -- login yolab https://cache.demycode.ovh "$ATTIC_AUTH_TOKEN"
          # Push the FULL closure of what THIS bucket built — ceph and every one
          # of its build dependencies included. An older version of this grepped
          # store paths out of the build log, which captured the .drv files but
          # not their realizable outputs, so ceph's output never reached the cache
          # and every node recompiled it from source on every update.
          #
          # Driven off the paths the build step actually produced, NOT off a fixed
          # list of toplevels. Naming the toplevels here would make every bucket
          # build an entire NixOS system just to have something to push — which
          # would hand the `lint` bucket, whose real work is a few seconds of
          # shellcheck, the slowest job in the workflow. The `nixos` bucket builds
          # `nixos-create` and `nixos-join`, and those checks ARE the yolab-ci and
          # yolab-ci-join toplevels, so the closure this pushes is the same one as
          # before.
          xargs -a /tmp/bucket-out-paths.txt nix path-info --recursive \
            | nix run nixpkgs#attic-client -- push yolab --stdin --no-closure

  build-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Set branch tag
        id: tag
        run: echo "value=$(echo '${{ github.ref_name }}' | sed 's|/|-|g')" >>
          $GITHUB_OUTPUT

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push wg-sidecar
        uses: docker/build-push-action@v6
        with:
          context: apps/wg-sidecar
          push: true
          tags: ghcr.io/demycode/wg-sidecar:${{ steps.tag.outputs.value }}-latest

      - name: Build and push wg-register
        uses: docker/build-push-action@v6
        with:
          context: apps/wg-register
          push: true
          tags: ghcr.io/demycode/wg-register:${{ steps.tag.outputs.value }}-latest

  charts:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    concurrency:
      group: charts-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.16.3

      - name: Resolve registry and version suffix
        run: |
          set -euo pipefail
          owner=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "REGISTRY=oci://ghcr.io/${owner}/charts" >> "$GITHUB_ENV"

          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "SUFFIX=" >> "$GITHUB_ENV"
          else
            tag=$(echo '${{ github.ref_name }}' | sed 's|[^0-9A-Za-z]|-|g')
            echo "SUFFIX=-${tag}" >> "$GITHUB_ENV"
          fi

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Publish the library chart
        run: |
          set -euo pipefail
          mkdir -p /tmp/pkg
          base=$(grep '^version:' apps/catalog/yolab-common/Chart.yaml | head -1 | awk '{print $2}' | tr -d '"')
          echo "LIB_VERSION=${base}${SUFFIX}" >> "$GITHUB_ENV"
          helm package apps/catalog/yolab-common --version "${base}${SUFFIX}" --destination /tmp/pkg
          for attempt in 1 2 3 4 5; do
            helm push /tmp/pkg/yolab-common-*.tgz "${REGISTRY}" && break
            sleep $((attempt * 15))
          done
          rm -f /tmp/pkg/yolab-common-*.tgz

      - name: Lint, package and push app charts
        id: push
        run: |
          set -uo pipefail
          failed=""
          for chart in apps/catalog/*/; do
            [ -f "$chart/Chart.yaml" ] || continue
            grep -q '^type: library' "$chart/Chart.yaml" && continue
            name=$(grep '^name:' "$chart/Chart.yaml" | head -1 | awk '{print $2}')
            base=$(grep '^version:' "$chart/Chart.yaml" | head -1 | awk '{print $2}' | tr -d '"')

            # Point the dependency at the version just published. Without this a branch
            # build vendors main's library and silently tests the wrong thing.
            sed -i "/- name: yolab-common/{n;s|version: .*|version: \"${LIB_VERSION}\"|}" "$chart/Chart.yaml"
            helm dependency update "$chart" >/dev/null

            # Every app must tear its tunnel down on uninstall — the shared pre-delete
            # hook is what does that (see yolab-common/templates/_uninstall.tpl). A
            # chart missing this leaks an orphaned tunnel on the platform every time
            # it's removed, silently, with nothing in the uninstall response to say so.
            grep -rq "yolab-common.uninstallHook" "$chart" || { failed="$failed $name(no-uninstall-hook)"; continue; }

            # Charts whose schema marks fields required legitimately fail lint against
            # default values, so lint with a filled-in set — the point is catching
            # template errors, not re-testing the schema.
            helm lint "$chart" \
              --set config.password=PlaceholderPw2026 \
              --set config.admin_password=PlaceholderPw2026 \
              --set config.admin_email=admin@example.com \
              --set config.app_secret=PlaceholderPw2026 \
              --set config.app_key=PlaceholderAppKey2026Placeholder \
              --set config.api_key=PlaceholderApiKey2026 \
              --set config.auth_secret_key=PlaceholderAuthSecretKey2026Placeholder \
              --set config.gateway_token=PlaceholderGatewayToken2026Placeholder \
              --set config.server_name=example \
              --set config.server_pass=PlaceholderPw2026 \
              --set config.subdomain=example || { failed="$failed $name(lint)"; continue; }

            helm package "$chart" --version "${base}${SUFFIX}" --destination /tmp/pkg

            # GitHub rate-limits creating new container packages, and each chart is its
            # own package: the first run of this pushed 7 of 19 before a hard 403. Retry
            # with backoff, and never let one chart's failure abort the rest.
            pushed=""
            for attempt in 1 2 3 4 5; do
              # Pushing an existing name:version is a no-op, so retries and re-runs are
              # both safe. Published versions are never deleted — a restore from an old
              # backup may need one.
              if helm push "/tmp/pkg/${name}-${base}${SUFFIX}.tgz" "${REGISTRY}"; then pushed=1; break; fi
              sleep $((attempt * 15))
            done
            [ -n "$pushed" ] || failed="$failed $name"
            sleep 3
          done
          # The Chart.yaml edits above are build-time only; never let them reach a commit.
          git checkout -- apps/catalog
          echo "failed=${failed}" >> "$GITHUB_OUTPUT"
          [ -z "$failed" ] || echo "::warning::failed:${failed}"

      - name: Generate the catalog manifest
        run: |
          set -euo pipefail
          registry_path="${REGISTRY#oci://ghcr.io/}"
          {
            echo "# Generated by .github/workflows/push.yml — do not edit."
            echo "#"
            echo "# The list of apps in the official catalog. Chart bytes live in the OCI"
            echo "# registry below; this file exists only because a registry cannot be"
            echo "# enumerated without credentials. Entries are verified present before"
            echo "# being listed."
            echo "apiVersion: yolab.io/v1"
            echo "registry: ${REGISTRY}"
            echo "charts:"
            for chart in apps/catalog/*/; do
              [ -f "$chart/Chart.yaml" ] || continue
              grep -q '^type: library' "$chart/Chart.yaml" && continue
              name=$(grep '^name:' "$chart/Chart.yaml" | head -1 | awk '{print $2}')
              base=$(grep '^version:' "$chart/Chart.yaml" | head -1 | awk '{print $2}' | tr -d '"')
              code=$(curl -sS -o /dev/null -w "%{http_code}" \
                "https://ghcr.io/token?service=ghcr.io&scope=repository:${registry_path}/${name}:pull")
              if [ "$code" = "200" ]; then
                echo "  - name: ${name}"
                echo "    version: \"${base}${SUFFIX}\""
              else
                echo "::warning::${name} is not readable in the registry (HTTP ${code}) — omitted" >&2
              fi
            done
          } > catalog.yaml
          cat catalog.yaml

      - name: Upload the branch catalog manifest
        if: github.ref != 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          # Branch charts are published but not discoverable — main's catalog.yaml is the
          # only one committed. Serve this artifact somewhere (or set
          # YOLAB_OFFICIAL_CHART_REPO at it) to install a branch's catalog on a test node.
          name: catalog-${{ github.ref_name }}
          path: catalog.yaml
          retention-days: 7

      - name: Commit the catalog manifest
        if: github.ref == 'refs/heads/main'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add catalog.yaml
          if git diff --cached --quiet; then
            echo "catalog unchanged"
            exit 0
          fi
          # [skip ci] because this push lands on main and would otherwise re-trigger.
          git commit -m "Update catalog manifest from ${GITHUB_SHA::7} [skip ci]"
          git push

      - name: Report unpublished charts
        if: steps.push.outputs.failed != ''
        run: |
          # Deliberately last: on main the manifest for everything that DID publish is
          # already committed, so a partial run still moves the catalog forward and the
          # next run retries the rest. Failing here only makes the gap visible.
          echo "charts that did not publish:${{ steps.push.outputs.failed }}"
          exit 1

  # Split out of build-iso, where they were a second step that could only start
  # once the ISO's squashfs had finished. Neither package is an input to the ISO
  # image, so nothing was making that ordering necessary — it was just two
  # unrelated builds sharing a runner.
  homelab-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Build homelab packages
        id: build_homelab
        run: |
          set -euo pipefail
          # Two calls, not one with two installables: the runner's /nix/store
          # persists between commands, so whatever these two share is realised
          # once regardless, and naming one output per call avoids depending on
          # the order `--print-out-paths` happens to print them in.
          UI_OUT=$(nix build path:.#homelab-ui --no-link --print-build-logs --print-out-paths)
          API_OUT=$(nix build path:.#homelab-api --no-link --print-build-logs --print-out-paths)
          echo "ui_store_path=$UI_OUT" >> "$GITHUB_OUTPUT"
          echo "api_store_path=$API_OUT" >> "$GITHUB_OUTPUT"

      - name: Push to the Nix cache
        env:
          ATTIC_AUTH_TOKEN: ${{ secrets.ATTIC_AUTH_TOKEN }}
        if: ${{ env.ATTIC_AUTH_TOKEN != '' }}
        run: |
          set -euo pipefail
          nix run nixpkgs#attic-client -- login yolab https://cache.demycode.ovh "$ATTIC_AUTH_TOKEN"
          nix run nixpkgs#attic-client -- push yolab \
            "${{ steps.build_homelab.outputs.ui_store_path }}" \
            "${{ steps.build_homelab.outputs.api_store_path }}"

  build-iso:
    runs-on: ubuntu-latest
    # needs: pre-commit
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Set branch tag
        id: tag
        run: echo "value=$(echo '${{ github.ref_name }}' | sed 's|/|-|g')" >>
          $GITHUB_OUTPUT

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Build ISO
        id: build_iso
        run: |
          # `path:.#`, NOT `.#`. These resolve to different flake sources — the
          # git flake copies only tracked files, `path:` copies the directory —
          # so their source hashes differ and so does every derivation built from
          # them. This job used `.#iso` while the checks used `path:.#ci`, which
          # meant local-api was two distinct derivations
          # (cn8cc3y… vs vnb0nah…) built from scratch on both runners, with
          # neither able to use the cache the other had just filled.
          ISO_OUT=$(nix build path:.#iso --no-link --print-build-logs --print-out-paths)
          echo "store_path=$ISO_OUT" >> "$GITHUB_OUTPUT"
          echo "ISO store path: $ISO_OUT"

      - name: Push to the Nix cache
        env:
          ATTIC_AUTH_TOKEN: ${{ secrets.ATTIC_AUTH_TOKEN }}
        if: ${{ env.ATTIC_AUTH_TOKEN != '' }}
        run: |
          set -euo pipefail
          nix run nixpkgs#attic-client -- login yolab https://cache.demycode.ovh "$ATTIC_AUTH_TOKEN"
          nix run nixpkgs#attic-client -- push yolab \
            "${{ steps.build_iso.outputs.store_path }}"

      - name: Get ISO info
        id: iso_info
        run: |
          ISO_PATH=$(readlink -f "${{ steps.build_iso.outputs.store_path }}"/iso/*.iso)
          ISO_NAME=$(basename "$ISO_PATH")
          echo "iso_path=$ISO_PATH" >> $GITHUB_OUTPUT
          echo "iso_name=$ISO_NAME" >> $GITHUB_OUTPUT
          echo "ISO built: $ISO_NAME"
          ls -lh "$ISO_PATH"

      - name: Create/Update Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.value }}-latest
          name: NixOS Homelab Installer — ${{ steps.tag.outputs.value }}
          files: ${{ steps.iso_info.outputs.iso_path }}
          body: |
            ## NixOS Homelab Installer ISO

            This ISO can be used to install the homelab configuration.

            **Usage:**
            - Download the ISO file
            - Write to USB drive using Rufus (Windows) or dd (Linux/macOS):
              ```bash
              sudo dd if=${{ steps.iso_info.outputs.iso_name }} of=/dev/sdX bs=4M status=progress && sync
              ```
            - Boot from USB and follow installation instructions

            **Branch:** `${{ github.ref_name }}`
            **Built from:** `${{ github.sha }}`
            **Built at:** ${{ github.event.head_commit.timestamp }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload ISO as artifact
        uses: actions/upload-artifact@v7
        with:
          name: nixos-homelab-installer-${{ steps.tag.outputs.value }}-${{ github.sha }}
          path: ${{ steps.iso_info.outputs.iso_path }}
          retention-days: 7

  # Was its own format.yml, triggered on every branch (`push: branches: ["**"]`)
  # plus workflow_dispatch. Folded in here to match how yolab-external's ci.yml
  # holds every job in one file. The `on: push:` at the top of this file has no
  # branch filter, so it already covers every branch the old file did — the one
  # gap is tags, which that trigger fires on but a PR base cannot be, hence the
  # `if:` below (the old file used `branches: ["**"]` specifically to exclude
  # them). workflow_dispatch (manual re-run without a push) was dropped rather
  # than added to every other job in this file for one job's sake.
  format:
    if: startsWith(github.ref, 'refs/heads/')
    concurrency:
      group: format-${{ github.ref_name }}
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      - name: Run nix fmt
        id: fmt
        run: |
          nix fmt
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Already formatted; nothing to open a PR for."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            git --no-pager diff --stat
          fi

      - name: Open a pull request
        if: steps.fmt.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v8
        with:
          # Fixed name per source branch, so a later push updates the open PR
          # rather than stacking a second one.
          branch: fmt-${{ github.ref_name }}
          base: ${{ github.ref_name }}
          delete-branch: true
          title: "style: nix fmt (${{ github.ref_name }})"
          commit-message: |
            style: nix fmt

            Produced by `nix fmt`, the same treefmt wrapper that
            `checks.formatting` gates on.
          labels: formatting
          body: |
            `nix fmt` produced changes against `${{ github.ref_name }}`.

            This is mechanical output — the same treefmt wrapper
            `checks.formatting` builds, so merging it is what makes
            `nix run .#ci` green on that branch.

            If this appeared without anyone editing the affected files, a
            formatter version moved: check the most recent bump to
            `treefmt-nix`, `nixpkgs` (prettier, shfmt, alejandra) or the
            pinned Rust toolchain (rustfmt).
