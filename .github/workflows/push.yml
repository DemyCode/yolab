name: push

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/boost "$AGENT_TOOLSDIRECTORY" \
            /usr/local/share/powershell /usr/share/swift
          sudo docker image prune --all --force || true
          df -h /

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes

      - uses: DeterminateSystems/magic-nix-cache-action@v8

      - name: Run all checks
        run: nix run .#ci --print-build-logs | tee /tmp/ci-output.txt

      # `nix run .#ci`'s own output is `✓ <name>  (<store path>)` per check — see
      # nix/checks.nix's `local-api-tests`/`clippy-local-api`/etc. and flake.nix's
      # `ci` app, which builds that line by interpolating each check's already-built
      # store path directly. Parsing it back out means every check this job already
      # built gets pushed — including local-api-tests and clippy-local-api, which
      # share almost their entire dependency closure with the local-api binary a
      # real node's `nixos-rebuild switch` compiles — with no second list to keep in
      # sync as checks are added or removed.
      #
      # Gated on the secret existing (not just non-empty — an absent repo secret and
      # an empty one both evaluate to "" in Actions) so a fork or a PR from someone
      # without cache-push access still gets a green check, just without populating
      # the cache.
      #
      # `secrets` isn't a valid context inside `if:` at all (GitHub rejects it with
      # "Unrecognized named-value: 'secrets'" at parse time, job- or step-level,
      # unconditionally) — env: is the documented way to get a secret's value
      # somewhere an `if:` can actually read it.
      - name: Push to the Nix cache
        env:
          ATTIC_AUTH_TOKEN: ${{ secrets.ATTIC_AUTH_TOKEN }}
        if: ${{ env.ATTIC_AUTH_TOKEN != '' }}
        run: |
          set -euo pipefail
          nix run nixpkgs#attic-client -- login yolab https://cache.demycode.ovh "$ATTIC_AUTH_TOKEN"
          mapfile -t paths < <(grep -oE '/nix/store/[^)]+' /tmp/ci-output.txt | sort -u)
          nix run nixpkgs#attic-client -- push yolab "${paths[@]}"

  build-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Set branch tag
        id: tag
        run: echo "value=$(echo '${{ github.ref_name }}' | sed 's|/|-|g')" >>
          $GITHUB_OUTPUT

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push wg-sidecar
        uses: docker/build-push-action@v6
        with:
          context: apps/wg-sidecar
          push: true
          tags: ghcr.io/demycode/wg-sidecar:${{ steps.tag.outputs.value }}-latest

      - name: Build and push wg-register
        uses: docker/build-push-action@v6
        with:
          context: apps/wg-register
          push: true
          tags: ghcr.io/demycode/wg-register:${{ steps.tag.outputs.value }}-latest

  charts:
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: write
      packages: write
    concurrency:
      group: charts-${{ github.ref }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.16.3

      - name: Resolve registry and version suffix
        run: |
          set -euo pipefail
          owner=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "REGISTRY=oci://ghcr.io/${owner}/charts" >> "$GITHUB_ENV"

          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "SUFFIX=" >> "$GITHUB_ENV"
          else
            tag=$(echo '${{ github.ref_name }}' | sed 's|[^0-9A-Za-z]|-|g')
            echo "SUFFIX=-${tag}" >> "$GITHUB_ENV"
          fi

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Publish the library chart
        run: |
          set -euo pipefail
          mkdir -p /tmp/pkg
          base=$(grep '^version:' apps/catalog/yolab-common/Chart.yaml | head -1 | awk '{print $2}' | tr -d '"')
          echo "LIB_VERSION=${base}${SUFFIX}" >> "$GITHUB_ENV"
          helm package apps/catalog/yolab-common --version "${base}${SUFFIX}" --destination /tmp/pkg
          for attempt in 1 2 3 4 5; do
            helm push /tmp/pkg/yolab-common-*.tgz "${REGISTRY}" && break
            sleep $((attempt * 15))
          done
          rm -f /tmp/pkg/yolab-common-*.tgz

      - name: Lint, package and push app charts
        id: push
        run: |
          set -uo pipefail
          failed=""
          for chart in apps/catalog/*/; do
            [ -f "$chart/Chart.yaml" ] || continue
            grep -q '^type: library' "$chart/Chart.yaml" && continue
            name=$(grep '^name:' "$chart/Chart.yaml" | head -1 | awk '{print $2}')
            base=$(grep '^version:' "$chart/Chart.yaml" | head -1 | awk '{print $2}' | tr -d '"')

            # Point the dependency at the version just published. Without this a branch
            # build vendors main's library and silently tests the wrong thing.
            sed -i "/- name: yolab-common/{n;s|version: .*|version: \"${LIB_VERSION}\"|}" "$chart/Chart.yaml"
            helm dependency update "$chart" >/dev/null

            # Charts whose schema marks fields required legitimately fail lint against
            # default values, so lint with a filled-in set — the point is catching
            # template errors, not re-testing the schema.
            helm lint "$chart" \
              --set config.password=PlaceholderPw2026 \
              --set config.admin_password=PlaceholderPw2026 \
              --set config.admin_email=admin@example.com \
              --set config.app_secret=PlaceholderPw2026 \
              --set config.app_key=PlaceholderAppKey2026Placeholder \
              --set config.api_key=PlaceholderApiKey2026 \
              --set config.auth_secret_key=PlaceholderAuthSecretKey2026Placeholder \
              --set config.gateway_token=PlaceholderGatewayToken2026Placeholder \
              --set config.server_name=example \
              --set config.server_pass=PlaceholderPw2026 \
              --set config.subdomain=example || { failed="$failed $name(lint)"; continue; }

            helm package "$chart" --version "${base}${SUFFIX}" --destination /tmp/pkg

            # GitHub rate-limits creating new container packages, and each chart is its
            # own package: the first run of this pushed 7 of 19 before a hard 403. Retry
            # with backoff, and never let one chart's failure abort the rest.
            pushed=""
            for attempt in 1 2 3 4 5; do
              # Pushing an existing name:version is a no-op, so retries and re-runs are
              # both safe. Published versions are never deleted — a restore from an old
              # backup may need one.
              if helm push "/tmp/pkg/${name}-${base}${SUFFIX}.tgz" "${REGISTRY}"; then pushed=1; break; fi
              sleep $((attempt * 15))
            done
            [ -n "$pushed" ] || failed="$failed $name"
            sleep 3
          done
          # The Chart.yaml edits above are build-time only; never let them reach a commit.
          git checkout -- apps/catalog
          echo "failed=${failed}" >> "$GITHUB_OUTPUT"
          [ -z "$failed" ] || echo "::warning::failed:${failed}"

      - name: Generate the catalog manifest
        run: |
          set -euo pipefail
          registry_path="${REGISTRY#oci://ghcr.io/}"
          {
            echo "# Generated by .github/workflows/push.yml — do not edit."
            echo "#"
            echo "# The list of apps in the official catalog. Chart bytes live in the OCI"
            echo "# registry below; this file exists only because a registry cannot be"
            echo "# enumerated without credentials. Entries are verified present before"
            echo "# being listed."
            echo "apiVersion: yolab.io/v1"
            echo "registry: ${REGISTRY}"
            echo "charts:"
            for chart in apps/catalog/*/; do
              [ -f "$chart/Chart.yaml" ] || continue
              grep -q '^type: library' "$chart/Chart.yaml" && continue
              name=$(grep '^name:' "$chart/Chart.yaml" | head -1 | awk '{print $2}')
              base=$(grep '^version:' "$chart/Chart.yaml" | head -1 | awk '{print $2}' | tr -d '"')
              code=$(curl -sS -o /dev/null -w "%{http_code}" \
                "https://ghcr.io/token?service=ghcr.io&scope=repository:${registry_path}/${name}:pull")
              if [ "$code" = "200" ]; then
                echo "  - name: ${name}"
                echo "    version: \"${base}${SUFFIX}\""
              else
                echo "::warning::${name} is not readable in the registry (HTTP ${code}) — omitted" >&2
              fi
            done
          } > catalog.yaml
          cat catalog.yaml

      - name: Upload the branch catalog manifest
        if: github.ref != 'refs/heads/main'
        uses: actions/upload-artifact@v7
        with:
          # Branch charts are published but not discoverable — main's catalog.yaml is the
          # only one committed. Serve this artifact somewhere (or set
          # YOLAB_OFFICIAL_CHART_REPO at it) to install a branch's catalog on a test node.
          name: catalog-${{ github.ref_name }}
          path: catalog.yaml
          retention-days: 7

      - name: Commit the catalog manifest
        if: github.ref == 'refs/heads/main'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add catalog.yaml
          if git diff --cached --quiet; then
            echo "catalog unchanged"
            exit 0
          fi
          # [skip ci] because this push lands on main and would otherwise re-trigger.
          git commit -m "Update catalog manifest from ${GITHUB_SHA::7} [skip ci]"
          git push

      - name: Report unpublished charts
        if: steps.push.outputs.failed != ''
        run: |
          # Deliberately last: on main the manifest for everything that DID publish is
          # already committed, so a partial run still moves the catalog forward and the
          # next run retries the rest. Failing here only makes the gap visible.
          echo "charts that did not publish:${{ steps.push.outputs.failed }}"
          exit 1

  build-iso:
    runs-on: ubuntu-latest
    # needs: pre-commit
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: Set branch tag
        id: tag
        run: echo "value=$(echo '${{ github.ref_name }}' | sed 's|/|-|g')" >>
          $GITHUB_OUTPUT

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes

      # --no-link throughout, and the ISO's store path captured as a step output.
      #
      # `nix build` without --out-link creates a symlink named `result`, so each
      # build here overwrote the last: `result` ended up pointing at
      # homelab-api, and the "Get ISO info" step below then looked for
      # result/iso/*.iso inside it and found nothing. That was latent for as
      # long as the cache-push step failed ahead of it and aborted the job
      # first — removing that step is what surfaced it.
      - name: Build ISO
        id: build_iso
        run: |
          ISO_OUT=$(nix build .#iso --no-link --print-build-logs --print-out-paths)
          echo "store_path=$ISO_OUT" >> "$GITHUB_OUTPUT"
          echo "ISO store path: $ISO_OUT"

      - name: Build homelab packages
        id: build_homelab
        run: |
          UI_OUT=$(nix build .#homelab-ui --no-link --print-build-logs --print-out-paths)
          API_OUT=$(nix build .#homelab-api --no-link --print-build-logs --print-out-paths)
          echo "ui_store_path=$UI_OUT" >> "$GITHUB_OUTPUT"
          echo "api_store_path=$API_OUT" >> "$GITHUB_OUTPUT"

      # The consuming side has now landed — homelab/nixos/common.nix's
      # nix.settings.substituters/trusted-public-keys — so this is no longer the
      # dead push the comment it replaced described. homelab-api is the one that
      # actually matters: it is the multi-minute Rust build every node repeats on
      # every `nixos-rebuild switch`, and unlike the full system closure (which
      # bakes in each node's own config.toml path) it is byte-identical across
      # every node, so one push here serves all of them.
      # `secrets` isn't a valid context inside `if:` at all (GitHub rejects it with
      # "Unrecognized named-value: 'secrets'" at parse time) — env: is the
      # documented way to get a secret's value somewhere an `if:` can read it.
      - name: Push to the Nix cache
        env:
          ATTIC_AUTH_TOKEN: ${{ secrets.ATTIC_AUTH_TOKEN }}
        if: ${{ env.ATTIC_AUTH_TOKEN != '' }}
        run: |
          set -euo pipefail
          nix run nixpkgs#attic-client -- login yolab https://cache.demycode.ovh "$ATTIC_AUTH_TOKEN"
          nix run nixpkgs#attic-client -- push yolab \
            "${{ steps.build_iso.outputs.store_path }}" \
            "${{ steps.build_homelab.outputs.ui_store_path }}" \
            "${{ steps.build_homelab.outputs.api_store_path }}"

      - name: Get ISO info
        id: iso_info
        run: |
          ISO_PATH=$(readlink -f "${{ steps.build_iso.outputs.store_path }}"/iso/*.iso)
          ISO_NAME=$(basename "$ISO_PATH")
          echo "iso_path=$ISO_PATH" >> $GITHUB_OUTPUT
          echo "iso_name=$ISO_NAME" >> $GITHUB_OUTPUT
          echo "ISO built: $ISO_NAME"
          ls -lh "$ISO_PATH"

      - name: Create/Update Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.value }}-latest
          name: NixOS Homelab Installer — ${{ steps.tag.outputs.value }}
          files: ${{ steps.iso_info.outputs.iso_path }}
          body: |
            ## NixOS Homelab Installer ISO

            This ISO can be used to install the homelab configuration.

            **Usage:**
            - Download the ISO file
            - Write to USB drive using Rufus (Windows) or dd (Linux/macOS):
              ```bash
              sudo dd if=${{ steps.iso_info.outputs.iso_name }} of=/dev/sdX bs=4M status=progress && sync
              ```
            - Boot from USB and follow installation instructions

            **Branch:** `${{ github.ref_name }}`
            **Built from:** `${{ github.sha }}`
            **Built at:** ${{ github.event.head_commit.timestamp }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload ISO as artifact
        uses: actions/upload-artifact@v7
        with:
          name: nixos-homelab-installer-${{ steps.tag.outputs.value }}-${{ github.sha }}
          path: ${{ steps.iso_info.outputs.iso_path }}
          retention-days: 7
