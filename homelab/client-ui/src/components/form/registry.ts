import {
  ArrayFieldTemplate,
  FieldTemplate,
  ObjectFieldTemplate,
} from "./templates";
import {
  CheckboxWidget,
  PasswordWidget,
  TextareaWidget,
  TunnelWidget,
} from "./widgets";

export const templates = {
  FieldTemplate,
  ObjectFieldTemplate,
  ArrayFieldTemplate,
};

export const widgets = {
  TunnelWidget,
  PasswordWidget,
  CheckboxWidget,
  TextareaWidget,
};
