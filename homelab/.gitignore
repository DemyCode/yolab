# Exclude ignored/ folder - contains user-specific config and hardware config
ignored/config.toml
ignored/hardware-configuration.nix

# NixOS build results
result
result-*

# Backup files
*.backup
*.bak
