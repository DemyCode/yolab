{
  "title": "Dawarich",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "dawarich"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    },
    "time_zone": {
      "type": "string",
      "title": "Time zone",
      "default": "UTC",
      "description": "e.g. Europe/Paris, America/New_York"
    }
  }
}
