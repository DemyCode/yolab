{
  "title": "Dawarich",
  "type": "object",
  "required": ["subdomain", "disk"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "dawarich"
    },
    "disk": {
      "type": "object",
      "title": "Storage disk",
      "format": "disk"
    },
    "time_zone": {
      "type": "string",
      "title": "Time zone",
      "default": "UTC",
      "description": "e.g. Europe/Paris, America/New_York"
    }
  }
}
