{
  "title": "Dawarich",
  "type": "object",
  "required": ["subdomain"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "dawarich"
    },
    "time_zone": {
      "type": "string",
      "title": "Time zone",
      "default": "UTC",
      "description": "e.g. Europe/Paris, America/New_York"
    }
  }
}
