{
  "title": "Immich",
  "type": "object",
  "required": ["subdomain"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "immich"
    },
    "db_password": {
      "type": "string",
      "x-auto": "password"
    }
  }
}
