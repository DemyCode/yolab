{
  "title": "Immich",
  "type": "object",
  "required": ["subdomain", "disk", "db_password"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "immich"
    },
    "disk": {
      "type": "object",
      "title": "Storage disk",
      "format": "disk"
    },
    "db_password": {
      "type": "string",
      "title": "Database password"
    }
  }
}
