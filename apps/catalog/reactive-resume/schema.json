{
  "title": "Reactive Resume",
  "type": "object",
  "required": ["subdomain", "disk"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "resume"
    },
    "disk": {
      "type": "object",
      "title": "Storage disk",
      "format": "disk"
    }
  }
}
