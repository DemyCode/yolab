{
  "title": "Reactive Resume",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "resume"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    }
  }
}
