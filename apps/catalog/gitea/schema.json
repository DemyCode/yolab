{
  "title": "Gitea",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "git"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    }
  }
}
