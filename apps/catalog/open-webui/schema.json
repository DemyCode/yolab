{
  "title": "Open WebUI",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "ai"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage",
      "description": "Models can be several GB each — allocate generously"
    }
  }
}
