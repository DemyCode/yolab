{
  "title": "LibreSpeed",
  "type": "object",
  "required": ["subdomain", "password"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "librespeed"
    },
    "password": {
      "type": "string",
      "title": "Admin password"
    }
  }
}
