{
  "title": "LibreSpeed",
  "type": "object",
  "required": ["subdomain", "storage", "password"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "speed"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    },
    "password": {
      "type": "string",
      "title": "Admin password"
    }
  }
}
