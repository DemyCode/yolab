{
  "title": "ntfy",
  "type": "object",
  "required": ["subdomain", "disk"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "ntfy"
    },
    "disk": {
      "type": "object",
      "title": "Storage disk",
      "format": "disk"
    }
  }
}
