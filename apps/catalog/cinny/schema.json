{
  "title": "Cinny",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "cinny"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    }
  }
}
