{
  "title": "Jellyfin",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "jellyfin"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    }
  }
}
