{
  "title": "Strfry",
  "type": "object",
  "required": ["subdomain", "disk"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "nostr"
    },
    "disk": {
      "type": "object",
      "title": "Storage disk",
      "format": "disk"
    }
  }
}
