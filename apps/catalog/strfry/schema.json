{
  "title": "Strfry",
  "type": "object",
  "required": ["subdomain", "storage"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "nostr"
    },
    "storage": {
      "type": "object",
      "title": "Storage",
      "format": "storage"
    }
  }
}
