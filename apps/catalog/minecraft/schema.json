{
  "title": "Minecraft",
  "type": "object",
  "required": ["subdomain", "disk"],
  "properties": {
    "subdomain": {
      "type": "string",
      "title": "Subdomain",
      "format": "tunnel",
      "default": "mc"
    },
    "disk": {
      "type": "object",
      "title": "Storage disk",
      "format": "disk"
    },
    "online_mode": {
      "type": "boolean",
      "title": "Online mode (Mojang auth)",
      "description": "Requires the server to reach Mojang's auth servers. Disable for IPv6-only environments.",
      "default": false
    },
    "difficulty": {
      "type": "string",
      "title": "Difficulty",
      "enum": ["peaceful", "easy", "normal", "hard"],
      "default": "normal"
    },
    "ops": {
      "type": "string",
      "title": "Operators (comma-separated usernames)"
    },
    "whitelist": {
      "type": "string",
      "title": "Whitelist (comma-separated usernames)"
    }
  }
}
